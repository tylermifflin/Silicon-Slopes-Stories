<!DOCTYPE html>
<html lang="en"> 

<head>
    <meta charset="UTF-8">
    <title>Silicon Slopes Stories</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" />
</head>

<body>
    <div class="flex-container">
        <header class="display-flex justify-space-between align-center">
            <h1>Silicon Slopes Stories</h1>
        </div>
        <nav class="display-flex justify-space-between align-center">
            <ul class="nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                {{#if logged_in}}
                <li class="nav-item">
                    <a class="nav-link" href="/dashboard">Add Story</a>
                    <button class="no-button" id="logout">Logout</button>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link" href="/login">Login or Sign Up</a>
                </li>
                {{/if}}
            </ul>
    <main class="container">
        <div class="row">
            <div class="col-md-12">
                {{{body}}}
            </div>
        </div>
    </main>
 <!-- show logout button for slooged in users only -->
{{#if logged_in}}
<script src="/js/logout.js"></script>
{{/if}}
</body>





























</html>